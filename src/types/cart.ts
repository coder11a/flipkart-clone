export type CartItem = {
  id: number;
  productSlug: string;
  title: string;
  brand: string;
  imageUrl: string;
  color: string;
  size: string;
  quantity: number;
  unitPrice: number;
  addedAt: string;
};
